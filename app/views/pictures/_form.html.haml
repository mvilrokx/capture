= simple_form_for @picture, :html => {:class => 'form-horizontal'}, :defaults => {:input_html => {:class => 'row span5'}} do |f|
  - if f.error_notification
    .alert.alert-error.fade.in
      %a.close{:data => {:dismiss => "alert"}, :href => "#"} &times;
      = f.error_notification

  %legend #{@picture.new_record? ? "Upload" : "Edit"} Image
  = f.input :title 
  = f.input :description, :input_html => { :rows => 4 }
  = f.input :captured_image
  .form-actions
    = f.button :submit, :class => 'btn-primary', :disable_with => "Uploading...", :value => "Upload"
    = link_to 'Cancel', pictures_path, :class => "btn btn-danger"